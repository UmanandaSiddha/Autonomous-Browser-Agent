# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

browser_profiles/
outputs/

.env
# Local SQLite database (contains password hashes)
browser_agent.db

# Training data derived from a real Gmail inbox - keep private.
# The prompts contain full message subjects and snippets, and the
# labels quote amounts, account numbers and OTPs from them.
training/data/*.jsonl
training/data/raw/

# Fine-tuning checkpoints and merged weights (large, and derived
# from the private data above).
training/outputs/
